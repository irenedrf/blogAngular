export interface Post {
    titulo: string;
    texto: string;
    autor: string;
    imagen: string;
    fecha: string; //date?? hay que hacer un pipe con el formato que quiera
    categoria: string;
}